// Iterables :
// In which we can use for of loop 
// TO which we can access using index
// example : string, array

const username = "Abimanyu";
 for(user of username){
     console.log(user);
 }

// arryay like object 
// which have length property 
// and we can access it using index 
// example : string, array

const userName = "Abimanyu";
console.log(userName.length); // 8
console.log(userName[4]);  // a